[package]
name = "larm"
version = "0.1.0"
authors = ["Eilif Hjermann Lindblad <eilifhl@stud.ntnu.no>"]
edition = "2024"

[dependencies]
image = { version = "0.25.9", default-features = false, features = ["jpeg", "png"] }
noise = { version = "0.9.0", features = ["std"] }
rayon = "1.11.0"

[lib]
crate-type = ["cdylib"]
